import 'package:flutter/material.dart';

class ScreenConsultation extends StatelessWidget {
  const ScreenConsultation({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Padding(
          padding: EdgeInsets.all(20), child: Text('Consultation Page')),
    );
  }
}
